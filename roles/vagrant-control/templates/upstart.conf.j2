description "{{ application_description }}"
start on runlevel [2345]
stop on runlevel [06]
respawn

env UWSGI=/usr/local/bin/uwsgi
env LOGTO=/var/log/{{ application_name }}/emperor.log

exec $UWSGI --master --emperor {{ home }}/vassals --die-on-term --uid {{ user }} --gid {{ user }} --logto $LOGTO
