---

- hosts: all
  sudo: true
  #accelerate: true

  roles:
    - firewall # make idempotent.
    - bootstrap # make fw rule for accelerate idempotent
    - common
    - nginx

    - nginx-api
    - htpasswd-api
    - vagrant-control
    #   - vagrant-worker
